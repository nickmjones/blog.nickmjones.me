<!DOCTYPE html>
<html>
  <head>
    <%= display_meta_tags site: "It's Probably Nothing" %>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=League+Spartan:wght@400;600&family=Source+Serif+Pro:ital,wght@0,300;0,400;0,700;1,300;1,400&display=swap" rel="stylesheet">
    <!-- plausible.io analytics -->
    <script defer data-domain="nickmjones.me" src="https://plausible.io/js/script.js">
    </script>
    <!-- end analytics -->
  </head>

  <body class="bg-lightyellow dark:bg-gray-800">

    <div id="flash">
      <% flash.each do |key, value| %>
        <div class="flash <%= key %>"><%= value %></div>
      <% end %>
    </div>

    <main class="flex flex-wrap md:flex-nowrap max-w-5xl mx-auto mt-9">

      <!-- left column p-10-->
      <div class="p-9 md:p-10 max-w-full md:w-4/6">
        <!-- masthead -->
        <div class="flex flex-col mb-28">
          <h1 class="text-5xl font-serif italic font-bold leading-tighter text-slate-700 dark:text-indigo-400 pb-2">
            <%= link_to "It's Probably Nothing", "/" %>
          </h1>
          <h3 class="text-md text-sky-500 uppercase tracking-wider">
            Written by Nick Jones
          </h3>
        </div>
        <!-- /masthead -->
        <%= yield %>
      </div>
      <!--/left column -->
      
      <!-- right column -->
      <%= render "shared/sidebar" %>
      <!-- /right column -->

    </main>
    
  </body>
</html>
